import { ThemedInpDirective } from './themed-inp.directive';

describe('ThemedInpDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemedInpDirective();
    expect(directive).toBeTruthy();
  });
});
